import type { MessageConstructor } from '../Message/Message';
export interface Capability {
    name: string;
    messageTypes?: MessageConstructor[];
    usesTags?: boolean;
}
export declare type ServerCapability = Capability & {
    param?: string;
};
